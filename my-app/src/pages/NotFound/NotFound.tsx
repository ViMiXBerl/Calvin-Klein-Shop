import { NotFoundProps } from "pages/NotFound/types";
import Text from "components/Text/Text";

const NotFound = ({ children }: NotFoundProps): any => {
	return <Text variant='h4'>NOT FOUND</Text>;
};

export default NotFound;
